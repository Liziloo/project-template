/**
 * @param {string} name
 * @param {number} id
 */
function initializeUser(name, id) {
  console.log(`User ${name} logged in with ID: ${id}`);
}

// Hover over 'initializeUser' below—VS Code will now show you 
// exactly what it needs, thanks to the Bridge!
initializeUser("Geordi", 101);
